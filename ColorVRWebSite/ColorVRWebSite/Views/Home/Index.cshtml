@model HomeViewModel

<h3>A Complete Virtual Reality Learning Platform</h3>

@await Html.PartialAsync("Carousel")

<h2 class="center_div container">Prefered Playgrounds of the Week</h2>
<h4 class="center_div container">Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var pg in Model.preferedPlayGrounds)
    {
        @Html.Partial("PlayGroundsSummary", pg)
    }
</div>